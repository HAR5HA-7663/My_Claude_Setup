{
  "_comment": "User-scope MCP servers (live in ~/.claude.json under mcpServers, or add via `claude mcp add --scope user`). Placeholders in <ANGLE_BRACKETS> must be filled in by the installing agent after asking the user. See INSTALL_AGENT.md for prerequisites per server.",
  "mcpServers": {
    "brain-fs": {
      "type": "stdio",
      "command": "/opt/homebrew/bin/mcp-server-filesystem",
      "args": [
        "<HOME>/brain"
      ],
      "_prereq": "Requires ~/brain to exist (personal LLM wiki — private, NOT in this repo). `npm i -g @modelcontextprotocol/server-filesystem` or keep the npx form. Skip if the user is not restoring the brain."
    },
    "excalidraw": {
      "type": "stdio",
      "command": "node",
      "args": [
        "<HOME>/tools/mcp_excalidraw/dist/index.js"
      ],
      "env": {
        "EXPRESS_SERVER_URL": "http://localhost:8765",
        "ENABLE_CANVAS_SYNC": "true",
        "PORT": "8765"
      },
      "_prereq": "Clone https://github.com/yctimlin/mcp_excalidraw.git into ~/tools/mcp_excalidraw, then `npm install && npm run build` so dist/index.js exists."
    },
    "gmessages": {
      "type": "sse",
      "url": "http://127.0.0.1:7007/mcp/sse",
      "_prereq": "Requires the openmessage daemon (Google Messages bridge, separate personal project) serving MCP on 127.0.0.1:7007. Skip unless the user runs it. Keep the settings.json permissions.deny entries for mcp__gmessages__send_* — read-only use."
    },
    "monday": {
      "type": "http",
      "url": "https://mcp.monday.com/mcp",
      "_prereq": "OAuth on first use — run /mcp inside Claude Code and authenticate in the browser."
    }
  }
}
