---
description: Convene the LLM Council on a hard decision. 5 advisors in parallel, anonymous peer review, Chairman synthesis, HTML + markdown output. Usage: /council <your question + context>
---

Invoke the `council` skill to run the full Karpathy-style LLM Council workflow on the following question.

Question and context from the user:

$ARGUMENTS

Follow the skill's workflow exactly — do not skip the parallel dispatch, the anonymous peer review, the letter-shuffling, or the HTML + markdown output files. If the user provided no question after `/council`, ask them for one before proceeding.
